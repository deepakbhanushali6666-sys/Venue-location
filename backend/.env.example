PORT=4000
CORS_ORIGIN=http://localhost:5173

# Public values - safe to share, already used by the frontend. No secret key is required:
# the backend forwards each caller's own Supabase JWT, so Postgres RLS enforces authorization.
SUPABASE_URL="https://ycgtzabcmvnhwetfyckw.supabase.co"
SUPABASE_ANON_KEY=
SUPABASE_PROJECT_ID="ycgtzabcmvnhwetfyckw"
# Optional - not read by current backend code. Only add if a future feature needs to bypass RLS.
SUPABASE_SECRET_KEY=

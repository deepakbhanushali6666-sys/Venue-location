Drop client testimonial photos here (`.jpg`, `.jpeg`, `.png` or `.webp`).

They are picked up automatically by `src/routes/gallery.tsx` via
`import.meta.glob` — no code changes needed, just add files and restart
the dev server (or rebuild) so Vite re-scans the folder.
